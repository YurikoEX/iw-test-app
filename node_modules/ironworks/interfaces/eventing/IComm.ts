
import ICommEventData = require('./ICommEventData');
import IInterceptor = require('../interception/IInterceptor');

import IWorker = require('../workers/IWorker');

interface IComm extends IWorker {
    prefix(): string;
    start(callback?: (e: Error) => void);
    intercept(event: ICommEventData|string): IInterceptor
}

export = IComm;
